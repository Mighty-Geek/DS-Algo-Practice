#include <bits/stdc++.h>
using namespace std;

string freqAlphabets(string str)
{
    string res;
    for (int i = 0; i < str.length(); i++)
    {
        }
}

int main()
{
    string str;
    cout << "Enter string to be decrypted : ";
    cin >> str;

    string res = freqAlphabets(str);
    cout << "Resultant string : " << res;
    return 0;
}